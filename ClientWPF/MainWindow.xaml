<Window x:Class="ClientWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:s="clr-namespace:ClientWPF.ServiceAgence"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Gestion de biens immobiliers"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <ObjectDataProvider x:Key="TransactionTypesProvider"
          MethodName="GetValues"
          ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="s:BienImmobilierBase+eTypeTransaction"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <Style x:Key="S_Button" TargetType="Button">
            <Setter Property="Background" Value="#00000000" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Width" Value="100"/>
        </Style>

        <!--<ControlTemplate x:Key="T_Button" TargetType="Button" >
            
            <Grid>
                <Grid.RowDefinitions>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image x:Name="Image" 
                       Source="images/add.png"
                           />
                <TextBox x:Name="Label"
                         Text="{TemplateBinding Content}"
                         BorderThickness="0"
                         TextAlignment="Justify"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                           Grid.Row="0"
                           Grid.Column="1"
                               />

            </Grid>
        </ControlTemplate>-->

    </Window.Resources>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="173*"/>
            <ColumnDefinition Width="207*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="21*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="137*"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="listBox" HorizontalAlignment="Left" Margin="10,0,0,10" Width="310" Background="#FFEEEEEE" Grid.Row="2"
          ItemsSource="{Binding ListBienBase}"
                 MouseDoubleClick="ListBox_MouseDoubleClick"
                 >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Titre}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <GridSplitter Grid.Row="2" HorizontalAlignment="Stretch" Margin="318,0,3,-10" />
        <Button x:Name="button_A" Content="Ajouter" Click="button_Add" Style="{StaticResource S_Button}"  Margin="10,10,0,10" Width="70" />
        <Button x:Name="button_M" Content="Modifier" Click="button_Modify" Style="{StaticResource S_Button}" Margin="85,10,0,10" Width="70" />
        <Button x:Name="button_D" Content="Supprimer" Click="button_Delete" Style="{StaticResource S_Button}" Margin="160,10,0,10" Width="70" />
        <Button x:Name="button_F" Content="Filtre" Click="button_Filtre" Style="{StaticResource S_Button}" Margin="247,10,0,10" Width="70" />

        <Grid Grid.Column="1" HorizontalAlignment="Left" Height="421" Margin="10,10,-16,0" Grid.Row="1" VerticalAlignment="Top" Width="420" Grid.RowSpan="2">
            <Label x:Name="label_ID" Content="Pas d'Item select" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
        </Grid>
        <ComboBox x:Name="comboBox" HorizontalAlignment="Left" Margin="10,0,0,0" Grid.Row="1" VerticalAlignment="Top" Width="120"
                  ItemsSource="{Binding Source={StaticResource TransactionTypesProvider}}" Height="22"/>


    </Grid>
</Window>
